Two Pointer 

left = 0 
right = len(arr)-1

while left < right: (here we camparison index not their value )

if arr[left] + arr[right] == target:
    return  [arr[left], arr[right]]

elif arr[left] + arr[right] < target:
    left += 1

else:
    right -= 1